@model List<OrderItemModel>


<section class="h-100 gradient-custom">
  <div class="container py-5">
    <div class="row d-flex justify-content-between my-4">
      <div class="col-md-8">
        <div class="card mb-4">
          <div class="card-header py-3">
            <h5 class="mb-0">Siparişlerim</h5>
          </div>
          <div class="card-body">
          
            @if (Model.Count == 0)
            {
              <div>Siparişiniz Bulunmamaktadır.</div>
              
            }
            else
            {
              @foreach (var item in Model)
              {
                @await Html.PartialAsync("_orderItem", item)
             
              }
            }
          </div>
        </div>
        
      </div>
    </div>
    
    </div>
</section>


                   @* @ViewBag.OrderStates[item.orderState] *@